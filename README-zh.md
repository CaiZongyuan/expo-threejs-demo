# Expo Three.js Demo 项目

这是一个使用 Expo 和 React Three Fiber 构建的 3D 演示项目，展示了在 React Native 中集成 Three.js 的能力。

## 📱 项目截图

![项目截图](/.github/resources/simple-mesh%20.jpg)

## ✨ 功能特性

- 🎲 **3D 立方体渲染** - 使用 React Three Fiber 渲染两个交互式 3D 立方体
- 🔄 **自动旋转动画** - 立方体会持续旋转，展示流畅的 3D 动画效果
- 🎯 **交互式体验** - 支持点击和悬停交互：
  - 点击立方体可以放大/缩小
  - 鼠标悬停时改变颜色
- 💡 **光照效果** - 包含环境光和点光源，营造逼真的 3D 视觉效果

## 🛠️ 技术栈

- **Expo** - React Native 开发框架
- **React Three Fiber** - Three.js 的 React 渲染器
- **Three.js** - 3D 图形库
- **TypeScript** - 类型安全的 JavaScript

## 🚀 快速开始

### 环境要求

- Node.js 18+
- Bun (现代 JavaScript 运行时和包管理器)
- Expo CLI
- Android Studio (Android 开发) 或 Xcode (iOS 开发)

### 安装依赖

```bash
bun install
```

### 运行项目

```bash
# 启动开发服务器
bun start

# 在 Android 设备上运行
bun run android

# 在 iOS 设备上运行
bun run ios

# 在浏览器中运行
bun run web
```

## 📁 项目结构

```
expo-threejs-demo/
├── app/
│   ├── index.tsx           # 主应用组件，包含 3D 场景
│   └── _layout.tsx         # 应用布局配置
├── .github/
│   └── resources/          # 项目资源文件
├── package.json            # 项目依赖配置
└── README.md              # 项目说明文档
```

## 🎮 使用说明

1. 启动应用后，你会看到两个橙色的 3D 立方体
2. 立方体会自动旋转
3. **点击立方体** - 立方体会放大 1.5 倍
4. **鼠标悬停** - 立方体颜色会变为粉红色

## 🧩 核心组件

### Box 组件

主要的 3D 立方体组件，具有以下特性：

- **自动旋转** - 使用 `useFrame` hook 实现每帧旋转
- **交互状态** - 管理 `hovered` 和 `active` 状态
- **事件处理** - 响应点击、悬停等用户交互

```typescript
// 核心功能代码片段
useFrame(() => {
  if (mesh.current) {
    mesh.current.rotation.x += 0.01;
    mesh.current.rotation.y += 0.01;
  }
});
```

## 🔧 自定义配置

你可以通过修改以下参数来自定义 3D 场景：

- **旋转速度** - 修改 `useFrame` 中的旋转增量
- **立方体大小** - 调整 `boxGeometry` 的参数
- **颜色方案** - 更改 `meshStandardMaterial` 的颜色值
- **光照位置** - 调整 `pointLight` 的位置坐标

## 🤝 贡献

欢迎提交 Issue 和 Pull Request 来改进这个项目！

## 📄 许可证

MIT License

## 🔗 相关链接

- [Expo 官方文档](https://docs.expo.dev/)
- [React Three Fiber 文档](https://docs.pmnd.rs/react-three-fiber/)
- [Three.js 文档](https://threejs.org/docs/)
